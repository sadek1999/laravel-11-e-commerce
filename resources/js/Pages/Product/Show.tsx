import { TProduct } from "@/types";

const Show = ({product}:{product:TProduct}) => {
  console.log(product)
  return (
    <div>
      <h1>this is product details page ........</h1>
    </div>
  );
};

export default Show;
